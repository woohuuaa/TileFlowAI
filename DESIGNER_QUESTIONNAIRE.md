# TileFlowAI 設計師需求確認問卷

這份問卷用來確認 TileFlowAI 在 AutoCAD 圖面中的辨識規則、排磚邏輯與估算需求。

請盡量依照實際 CAD 作圖方式填寫，若有範例圖檔、圖層命名規則或 block 清單，請一併提供。

## A. 基本資訊

1. 專案名稱：
2. 填寫人：
3. 日期：
4. CAD 軟體與版本：
5. 圖面單位：`mm / cm / m / 其他`
6. 本次主要要處理的空間類型：`客廳 / 玄關 / 浴室 / 廚房 / 陽台 / 其他`

## B. 鋪貼區域定義

1. 鋪貼區域要以什麼作為邊界？`封閉 polyline / 完成面 / 牆中心線 / 其他`
2. 每個要鋪貼的空間，是否都有獨立且封閉的輪廓？`是 / 否`
3. 如果沒有封閉輪廓，目前圖面通常是如何表示房間邊界？
4. 牆線代表的是什麼？`結構牆外框 / 牆厚 / 完成面 / 不一定`
5. 相鄰空間之間是否需要視為不同鋪貼區域？`是 / 否`
6. 是否會有一個空間內同時存在多個不連續鋪貼區域？`是 / 否`

## C. CAD 圖面規則

1. 牆、門、柱、設備、櫃體是否有固定的圖層命名規則？`有 / 沒有`
2. 若有，請列出常用 layer 名稱：
3. 門、柱、設備是否以 block 表示？`是 / 否 / 不一定`
4. 若是 block，請列出常用 block name：
5. 圖面中是否常出現 `xref`、外部參考或多來源圖檔合併情況？`是 / 否`
6. 不同設計師的作圖習慣是否一致？`一致 / 部分一致 / 不一致`
7. 圖面內是否常出現弧線牆、斜牆、圓角或異形空間？`是 / 否`

## D. 需要辨識的固定物件

1. 哪些物件需要從鋪貼面積中扣除？`柱子 / 管道間 / 設備基座 / 櫃體底座 / 浴缸 / 淋浴區 / 其他`
2. 上述物件在 CAD 中通常如何表示？`block / polyline / hatch / 只有符號 / 其他`
3. 這些物件是否大多具有封閉輪廓？`是 / 否 / 不一定`
4. 哪些物件只需要當作排版參考，不需要扣除面積？`門洞 / 門框 / 門檻 / 中心線 / 主視覺牆 / 其他`
5. 哪些物件應該完全忽略？`家具 / 活動櫃 / 裝飾線 / 註記 / 其他`
6. 門片是否會影響排磚？`會扣除面積 / 只作為起鋪參考 / 完全忽略`
7. 門框是否會影響排磚？`會扣除面積 / 只作為起鋪參考 / 完全忽略`
8. 門檻是否會影響排磚？`會扣除面積 / 只作為起鋪參考 / 完全忽略`
9. 是否有必須特別避開小碎磚的位置？`門口 / 走道 / 視覺中心 / 其他`

## E. 磁磚規格與排版規則

1. 第一版是否只需要支援長方形磁磚？`是 / 否`
2. 常用磁磚尺寸有哪些？
3. 是否需要把縫寬納入計算？`是 / 否`
4. 常用縫寬是多少？
5. 第一版是否只比較 `直貼` 與 `橫貼`？`是 / 否`
6. 是否需要考慮不同起鋪偏移？`是 / 否`
7. 起鋪基準通常以哪裡為準？`牆邊 / 門口 / 空間中心 / 指定基準線 / 其他`
8. 是否要求相鄰空間之間對縫？`是 / 否 / 視情況`
9. 最小可接受碎片尺寸是多少？
10. 是否有某些邊角即使可鋪，也不希望出現過小碎片？`是 / 否`

## F. 最佳方案判斷標準

請依重要性排序，1 為最重要。

1. 裁切磚數最少：
2. 最小碎片尺寸最大：
3. 廢料面積最少：
4. 視覺對稱：
5. 門口或主要視線處不要出現小碎磚：
6. 總消耗片數最少：

## G. 數量估算與下單規則

1. 輸出結果需要包含哪些欄位？`完整磚數 / 裁切磚數 / 總消耗片數 / 建議下單片數 / 箱數 / 其他`
2. 是否需要換算箱數？`是 / 否`
3. 每箱片數是否固定？`是 / 否 / 依品牌尺寸而定`
4. 備料率通常抓多少？`5% / 8% / 10% / 其他`
5. 裁切後的剩料是否允許重用？`同一空間可重用 / 跨空間可重用 / 一律不重用`
6. 第一版是否可接受保守算法：每個裁切磚先視為消耗一整片原磚？`是 / 否`

## H. 圖面輸出需求

1. 系統是否需要先畫預覽，再由使用者確認後正式落圖？`是 / 否`
2. 完整磚與裁切磚是否需要不同顏色顯示？`是 / 否`
3. 是否需要把結果畫到獨立圖層？`是 / 否`
4. 圖層名稱是否有公司標準？`有 / 沒有`
5. 是否需要在圖面上標註統計摘要？`是 / 否`
6. 是否需要匯出報表？`CSV / Excel / PDF / 不需要`

## I. Agent 自動辨識需求

1. 希望 Agent 自動辨識哪些物件？`牆 / 門 / 柱 / 設備 / 櫃體 / 排除區 / 其他`
2. 若圖層或 block 命名不一致，是否接受 Agent 先推測、再由設計師確認？`是 / 否`
3. 哪些物件最不能判錯？
4. 哪些物件即使漏判，也可以接受由人工補選？
5. 若辨識結果有不確定物件，是否接受系統列出候選清單請使用者勾選？`是 / 否`

## J. 測試資料提供

1. 是否可提供至少 3 份代表性 DWG 測試圖？`是 / 否`
2. 是否可提供 1 份規則清楚的標準圖？`是 / 否`
3. 是否可提供 1 份圖層命名較混亂的實際圖？`是 / 否`
4. 是否可提供 1 份含異形空間或柱位的圖？`是 / 否`
5. 是否可提供目前人工估算的對照結果？`是 / 否`

## K. 補充說明

1. 還有哪些現場施工或設計習慣，會影響排磚或下單？
2. 有沒有哪一類空間或磁磚尺寸，是第一版一定要優先支援的？
3. 有沒有哪類案例第一版可以先不處理？

## 建議附件

1. 代表性 DWG 圖檔 3 份以上
2. 常用圖層命名規則
3. 常用 block 名稱清單
4. 常用磁磚尺寸與品牌箱入數資料
5. 目前人工排磚或估算的範例
